# TopoKit Guardrails Configuration
# Generated by topokit-cli init --template=rag-system

{
  determinism: {
    temperature: 0.2,
    seed: session-hash,
    canonical_sort: true
  },
  confidence: {
    threshold: 0.7
  },
  fallback: {
    enabled: true,
    policy: rule_first
  }
}
